{
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "lint": "turbo lint",
    "test": "turbo run test",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "deploy": "gh-pages -d ./apps/web-ui/dist",
    "start": "concurrently  \"nx serve web-ui\"  \"nx serve web-api-local\"",
    "build": "concurrently \"nx build web-api-local\"",
    "build:ui": "nx build web-ui"
  },
  "devDependencies": {
    "@google-cloud/speech": "^7.2.1",
    "@nestjs/cli": "^10.0.0",
    "@types/node": "^22.0.0",
    "eslint": "^8.57.0",
    "ffmpeg-static": "^5.3.0",
    "gh-pages": "^6.1.1",
    "prettier": "^3.2.5",
    "turbo": "^2.0.9"
  },
  "packageManager": "pnpm@8.15.6",
  "name": "with-vite",
  "dependencies": {
    "@fastify/compress": "7.0.3",
    "fastify": "4.29.1"
  },
  "resolutions": {
    "fastify": "4.29.1"
  }
}
